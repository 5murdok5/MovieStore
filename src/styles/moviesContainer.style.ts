import styled from 'styled-components'

export const ContMovies = styled.div`
   display: flex; 
   flex-flow: row wrap;
   justify-content: center;
   padding: 10px 20px;
   flex-grow: calc(100%);
`

export const ContMoviesCart = styled.div`
   display: flex; 
   flex-flow: row wrap;
   padding: 10px 20px;
   flex-grow: calc(100%);
`